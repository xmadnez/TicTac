# TicTac running local

install (tested on python 3.11.9)

```
pip install torch torchvision torchaudio
pip install numpy
pip install pygame

```

after that start game by running

```
python main.py
```
